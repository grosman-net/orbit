#!/bin/bash
set -e

# Stop and disable service before removal
if systemctl is-active --quiet orbit 2>/dev/null; then
    systemctl stop orbit
fi

if systemctl is-enabled --quiet orbit 2>/dev/null; then
    systemctl disable orbit
fi

exit 0

